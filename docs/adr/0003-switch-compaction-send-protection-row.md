# Switch compaction chrome is a Send protection B-row

Switch compaction is send-time check-and-compact with the previous model. It is not a summarization route, not `/compact`, and not global compaction policy. Settings shows it as its own 发送保护 / Send protection group containing one B-row (label, hint, instant switch), default on. It does not sit inside the Subagent RouteCard.
